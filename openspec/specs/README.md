# OpenSpec Base Specs

No base domain specs have been archived yet.

Create active change specs under `openspec/changes/{change-name}/specs/{domain}/spec.md`, then merge accepted requirements here during `sdd-archive`.
